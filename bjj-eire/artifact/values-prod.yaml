bjj-mongodb:
  mongodb:
    image:
      tag: "8.2"
      pullPolicy: IfNotPresent
    resources:
      limits:
        cpu: 2000m
        memory: 2Gi
      requests:
        cpu: 1000m
        memory: 1Gi
    persistence:
      storageClassName: managed-premium
      size: 50Gi
bjj-api:
  api:
    image:
      repository: ghcr.io/ianoflynnautomation/bjjeire-api
      pullPolicy: IfNotPresent
      tag: ""
    replicaCount: 2
    podAnnotations:
      prometheus.io/scrape: "true"
      prometheus.io/path: "/metrics"
      prometheus.io/port: "8080"
    env:
      BJJEIRE_CORS_ALLOWEDORIGINS_0: "https://bjjeire.com"
      BJJEIRE_CORS_ALLOWEDORIGINS_1: "https://www.bjjeire.com"
      RATE_LIMIT_ENABLED: "true"
      RATE_LIMIT_PERMIT_LIMIT: "5"
      RATE_LIMIT_WINDOW_SECONDS: "10"
      RATE_LIMIT_REJECTION_STATUS_CODE: "429"
      READ_ONLY_MODE_ENABLED: "true"
      FEATURE_BJJ_EVENTS: "true"
      FEATURE_GYMS: "true"
      FEATURE_STORES: "true"
      FEATURE_COMPETITIONS: "true"
      DONATION_BITCOIN_ADDRESS: ""
      ENTRA_ISSUER_URI: "https://login.microsoftonline.com/REPLACE_WITH_YOUR_TENANT_ID/v2.0"
      ENTRA_AUDIENCE: "api://REPLACE_WITH_YOUR_CLIENT_ID"
      ENTRA_AUDIENCE_URI: "api://REPLACE_WITH_YOUR_CLIENT_ID"
    resources:
      limits:
        cpu: 1000m
        memory: 1Gi
      requests:
        cpu: 500m
        memory: 512Mi
    autoscaling:
      enabled: true
      minReplicas: 2
      maxReplicas: 10
      targetCPUUtilizationPercentage: 70
    podDisruptionBudget:
      enabled: true
      minAvailable: 1
  ingress:
    annotations:
      nginx.ingress.kubernetes.io/ssl-redirect: "true"
      nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
      nginx.ingress.kubernetes.io/backend-protocol: "HTTP"
      cert-manager.io/cluster-issuer: "letsencrypt-prod"
    hosts:
      - host: api.bjjeire.com
        paths:
          - path: /
            pathType: Prefix
    tls:
      - secretName: bjj-prod-api-tls
        hosts:
          - api.bjjeire.com
  azure:
    enabled: true
    workloadIdentity:
      enabled: true
bjj-frontend:
  frontend:
    image:
      repository: ghcr.io/ianoflynnautomation/bjjeire-frontend
      pullPolicy: IfNotPresent
      tag: ""
    replicaCount: 2
    podAnnotations:
      prometheus.io/scrape: "true"
      prometheus.io/path: "/metrics"
      prometheus.io/port: "80"
    env:
      NODE_ENV: production
      PORT: "80"
      SERVICES_API_HTTP_0: "http://bjj-api:8080"
    resources:
      limits:
        cpu: 500m
        memory: 512Mi
      requests:
        cpu: 200m
        memory: 256Mi
    autoscaling:
      enabled: true
      minReplicas: 2
      maxReplicas: 6
      targetCPUUtilizationPercentage: 70
      targetMemoryUtilizationPercentage: 80
    podDisruptionBudget:
      enabled: true
      minAvailable: 1
  ingress:
    annotations:
      nginx.ingress.kubernetes.io/ssl-redirect: "true"
      nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
      cert-manager.io/cluster-issuer: "letsencrypt-prod"
    hosts:
      - host: bjjeire.com
        paths:
          - path: /
            pathType: Prefix
      - host: www.bjjeire.com
        paths:
          - path: /
            pathType: Prefix
    tls:
      - secretName: bjj-prod-frontend-tls
        hosts:
          - bjjeire.com
          - www.bjjeire.com
seeder:
  # Never auto-seed prod. Run seeding as a deliberate, one-off manual Job.
  enabled: false
  force: false
  environment: Production
  dataset: "production"
  image:
    repository: ghcr.io/ianoflynnautomation/bjjeire-seeder
    pullPolicy: IfNotPresent
    tag: "0.1.6"
